# ResuPrep AI Environment Configuration
# Replace with your actual Groq API Key to enable out-of-the-box AI features.
# If left empty, the application will use the browser's localStorage key if provided by the user.
VITE_GROQ_API_KEY=your_groq_api_key_here
